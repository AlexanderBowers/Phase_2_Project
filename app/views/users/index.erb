<div class="user-list">
   <h1>Users</h1>
   <ul>
      <% @users.each do |user| %>
         <li><%= link_to user[:username], user_path(user) %></li>
      <% end %>
   </ul>
   <%= link_to "New User", "/new" %>
</div>
